# INSTRUCTION SET: Core Metadata & Environment

This chat will focus **only** on the Core Metadata & Environment section of TradeBot-001.

Scope includes:
- VERSION.md, CHANGELOG.md, README.md
- .env, .env_bot, .env_bot.enc
- Encryption tools: encrypt_password.py, encrypt_env_bot.py

Build rules:
- Follow master build document from /tbot_bot/config/instructions/tbot_master_build_instructions.txt
- No hardcoded values; all configs pulled from .env or .env_bot
- Encryption setup must align with ENCRYPTION_KEY management
- Validate version alignment: .env_bot[VERSION_TAG] must match VERSION.md
- Documentation must reflect system architecture, usage, and environment dependencies

Chat behavior:
- Only work on one file or task at a time
- Wait for explicit user confirmation (e.g., "continue") before proceeding
- Print each file or config block fully and labeled (no diffs or summaries)

Focus:
- Secure configuration loading
- Correct metadata management
- Clear documentation alignment to system behavior

