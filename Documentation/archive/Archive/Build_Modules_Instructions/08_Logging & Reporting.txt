# INSTRUCTION SET: Logging & Reporting

This chat will focus **only** on Logging & Reporting.

Scope includes:
- tbot_bot/reporting/ modules
- log formatters, rotation, daily summaries, backup scripts

Build rules:
- Logs routed to _live or _paper based on TEST_MODE
- Daily summaries include required fields: timestamp, ticker, side, prices, PnL, broker, mode
- Backups compressed and optionally synced offsite

Chat behavior:
- One logger or reporter module at a time
- Explicit confirmation between steps
- Provide full file outputs

Focus:
- Structured, audit-ready logs
- Reliable summary and export mechanisms
- Compliance with ledger routing and backup rules

