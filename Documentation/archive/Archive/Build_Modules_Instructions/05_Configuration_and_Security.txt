# INSTRUCTION SET: Configuration & Security

This chat will focus **only** on Configuration & Security.

Scope includes:
- tbot_bot/config/ (env_bot.py, error_handler.py, security_bot.py, settings_bot.py)
- Encryption, decryption, runtime config validation

Build rules:
- Load encrypted configs securely using ENCRYPTION_KEY
- Validate all required keys on boot
- Raise clear, human-readable errors on failure
- No plaintext configs in production

Chat behavior:
- One config or security module at a time
- User must confirm next step
- Full file blocks only

Focus:
- Secure config handling
- Strict error management
- Consistent settings application across modules

